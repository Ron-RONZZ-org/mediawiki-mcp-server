name: Publish Image

on:
  push:
    tags: ['v*']
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v6
        with:
          # The fork's own namespace — the upstream image name would fail the
          # push (fork tokens cannot write to ProfessionalWiki's ghcr) and
          # must never be overwritten by a fork build.
          images: ghcr.io/ron-ronzz-org/mediawiki-mcp-server
          labels: |
            org.opencontainers.image.title=MediaWiki MCP Server (Ron-RONZZ-org fork)
            org.opencontainers.image.description=Model Context Protocol (MCP) server for MediaWiki with EmbeddableContent/WikibaseCitation packs
            org.opencontainers.image.authors=Ron-RONZZ-org
            org.opencontainers.image.url=https://github.com/Ron-RONZZ-org/mediawiki-mcp-server
            org.opencontainers.image.licenses=MIT
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=edge,enable=${{ github.ref == 'refs/heads/master' }}
            type=sha,format=short

      - id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            GIT_SHA=${{ github.sha }}
          sbom: true
          provenance: mode=max

      - uses: actions/attest-build-provenance@v4
        with:
          subject-name: ghcr.io/professionalwiki/mediawiki-mcp-server
          subject-digest: ${{ steps.build.outputs.digest }}

      - if: startsWith(github.ref, 'refs/tags/v')
        uses: sigstore/cosign-installer@v4.1.2

      - if: startsWith(github.ref, 'refs/tags/v')
        run: cosign sign --yes ghcr.io/professionalwiki/mediawiki-mcp-server@${{ steps.build.outputs.digest }}
